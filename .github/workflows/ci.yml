name: CI
on:
  push:
    branches: [main]
  pull_request:
jobs:
  build-and-run:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Toolchain
        run: swift --version
      - name: Build and run the demo (self-checking)
        run: swift run
